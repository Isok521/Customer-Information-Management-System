# 第一阶段验证记录

日期：2026-09-13。全部测试使用虚构数据；未连接真实Supabase、未迁移远端数据库、未发布网站。

## 自动验证

- Next.js生产构建通过，页面包含首页、客户列表/详情/建档、服务列表/详情/新建、套餐权益、跟进事项。
- TypeScript严格检查通过。
- 6组测试通过：跨技师接力摘要、姓名/手机搜索、建立服务与重复拦截、错误套餐/过期/无余额拒绝、无套餐服务与必填验证、PostgreSQL约束/扣次/RLS。
- Schema使用PGlite实际执行PostgreSQL迁移。测试仅替换Supabase Auth为本地stub，省略pgcrypto扩展声明（PGlite已有gen_random_uuid）；其余迁移原样执行。
- 验证未完成不扣次、完成扣一次、余额不足时服务插入回滚、已完成记录禁止覆盖；匿名不可读、未登记/停用员工查不到客户、员工不可提升自身role。
- 此测试不代表真实Supabase部署或多连接并发测试已完成。

## 浏览器检查

- 桌面1440px检查工作台、客户接待摘要、时间线及侧边权益布局。
- 全局手机号搜索13800001001可进入陈玉兰档案；姓名搜索周志明可进入对应档案。
- 健康、权益、跟进Tabs切换正常，主摘要持续可见。
- 陈玉兰摘要显示最近完成服务为09-10李敏，而09-13王静服务仍在进行中；权益剩1次，与两笔使用历史一致。
- 新建服务切换为腰背养护时，仅出现匹配的腰背养护体验卡。
- 已为虚构客户周志明保存一条“演示验收”服务前记录；刷新后仍可读取，未扣次。此记录只在当前预览浏览器的本地演示中，源代码Mock保持原始样例。
- 客户列表无搜索结果时显示清除筛选入口；使用已有手机号快速建档被拦截，未生成重复客户。
- 834px平板视口检查通过，页面宽度等于视口宽度，宽表仅在自身容器横向滚动；图标导航保留可访问名称。

## 尚未实施

Supabase Auth与远端Repository、完整问卷编辑、服务即时记录写入、结束服务事务、购买/退款、跟进结果编辑、审计与生产并发测试。上述边界在README、产品设计和对应页面中明确说明。
